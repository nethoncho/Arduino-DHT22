language: python
python:
    - "2.7"

# Cache PlatformIO packages using Travis CI container-based infrastructure
sudo: false
cache:
    directories:
        - "~/.platformio"

env:
    - PLATFORMIO_CI_SRC=examples/Serial/Serial.ino

install:
    - pip install -U platformio

script:
    - platformio ci --lib=/var/folders/nj/nt38kxwd2xxfl5l42np_w7qw0000gn/T/platformiator-9HTUZ4-lib/project --board=uno --board=megaatmega1280
